# 面试话术库题库扩充计划

## 目标

把题库从“已有知识点索引”扩成可以应对真实前端面试的完整准备材料：

1. 通用知识覆盖常见概念、代码题、排错题、设计题和工程实践。
2. 三个项目分别形成独立、可追溯、能从业务讲到代码的项目题库。
3. 每道题都区分“做过”“看过”“理解原理但没有实践”，不编造经历、指标、上线结果或第三方接口效果。
4. 继续保持 `default` 与 `aaron` 的题库隔离。通用题可以重复，但个人身份、项目名称、代码路径、项目经验和代码证据不能跨用户复制。
5. 新增内容优先写成自然的口语回答，再补追问、边界、验证方式和代码证据。

## 当前盘点

当前 `default` 题库有 137 道题：

- 个人与求职：7
- HTML / CSS：23
- JavaScript：18
- React：13
- Vue：25
- 浏览器、网络与工程化：34
- 三个项目：15
- AI 与 Agent：2

已有题库的优点是项目核心链路已经有一定深度：轻购覆盖推荐、购物车和订单确认；城市视图覆盖图表、仪表盘、地图、3D、权限和 Redux；智服工单覆盖 SLA、状态机、并发和权限。

主要缺口不是继续增加同义概念，而是以下几类：

- JavaScript 的异步、执行模型、模块、错误处理和代码实现题不够完整。
- TypeScript 只有日常使用题，缺少泛型、类型体操边界、运行时校验和 API 类型设计。
- React 缺少状态快照、批处理、Effect 边界、并发渲染、性能排查、表单和数据请求设计。
- Vue 缺少组合式函数设计、响应式边界、组件更新、异步组件、性能和表单实践。
- HTML / CSS 缺少可访问性、布局实战、层叠上下文、现代选择器、容器查询和性能排错。
- 浏览器与工程化缺少安全、缓存细节、Service Worker、Web Worker、WebSocket / SSE、监控、测试和发布排障。
- 缺少前端代码题、手写题、测试题、系统设计题和行为面试题。
- 项目题仍应从“项目介绍”继续扩展到数据模型、接口契约、异常、权限、安全、性能、测试、部署和取舍。

参考范围以官方文档和项目代码为准：React 官方的 state snapshot 与 Effect 边界、Vue 官方的响应式和性能指南、TypeScript Handbook 的 narrowing 与 generics、MDN 的浏览器 API、W3C 的键盘和对话框可访问性规范，以及 Playwright 的用户可见行为测试原则。

## 第一阶段：通用知识补齐

每个主题建议新增 1 道主问题，配 3～6 道有答案追问。重复已有题目时，优先把内容补强到已有文件，不另建同义文件。

### JavaScript 运行时与语言

- 执行上下文、词法环境、作用域链和变量环境分别是什么？
- `let`、`const` 的暂时性死区与变量提升如何解释？
- `var` 闭包循环为什么拿到相同值，如何修复？
- 普通函数、箭头函数、类方法的 `this` 在不同调用方式下如何变化？
- `new` 运算符具体做了什么？手写 `new`。
- `instanceof` 如何沿原型链判断？如何手写一个简化版？
- `Object.create`、构造函数、`class extends` 的关系。
- 属性读取、原型属性、getter / setter 和 `hasOwn` 的优先级。
- `Proxy` 与 `Reflect` 的常见用法、限制和副作用。
- `Object.freeze`、`seal`、浅拷贝和深拷贝的边界。
- `structuredClone` 能复制什么，不能复制什么？
- Symbol、迭代器、可迭代对象、生成器和 `for...of` 的关系。
- Map、Set、WeakMap、WeakSet 的使用场景和垃圾回收差异。
- CommonJS 与 ESM 的加载时机、循环依赖和默认导出差异。
- 动态 `import()`、顶层 `await` 和代码分割。
- `AbortController` 如何取消 fetch，取消后 Promise 如何表现？
- 错误对象、`throw`、`finally`、未处理 rejection 的处理边界。
- 微任务、宏任务、MutationObserver、渲染机会的执行顺序。
- Promise resolution procedure：thenable、同化和链式错误传递。
- `Promise.all`、`allSettled`、`any`、`race` 的实际选型。
- 如何写一个带并发数限制的 Promise pool？
- 如何写一个可取消、可重试、带超时的请求封装？
- 如何处理请求去重、缓存、过期和旧响应覆盖？
- 手写 debounce / throttle，并说明 leading、trailing、取消和 flush。
- 手写 EventEmitter，处理 once、取消订阅和异常隔离。
- 手写深拷贝、数组扁平化、对象路径读写、LRU 缓存。

### TypeScript

- `unknown`、`any`、`never`、`void` 的区别和使用边界。
- 联合类型、交叉类型、可辨识联合和穷尽检查。
- 类型收窄、用户自定义 type guard、`asserts`。
- 泛型约束、默认类型参数、`keyof`、`typeof`、索引访问类型。
- `infer`、条件类型、映射类型、模板字面量类型的实际用途。
- `interface` 与 `type` 的扩展、声明合并和工程取舍。
- `satisfies` 与类型断言的区别。
- `as const`、只读数组、不可变更新的类型表达。
- 函数重载、可变参数元组和回调参数逆变。
- `Record`、`Pick`、`Omit`、`Partial`、`Required`、`ReturnType` 的实际用法。
- API 响应如何从 `unknown` 安全解析成领域类型？
- TypeScript 类型为什么不能替代运行时校验？
- 如何设计分页、错误、加载态和空态的状态联合类型？
- tsconfig 中 strict、module、target、paths、noUncheckedIndexedAccess 的影响。
- 声明文件、第三方库类型不准确时如何补充？
- 类型错误与业务错误如何分开处理？

### HTML、CSS 与可访问性

- 浏览器默认样式、继承、层叠、`!important` 和 @layer 的优先级。
- stacking context 如何形成？`z-index` 为什么有时不生效？
- containing block 如何影响 absolute、fixed、sticky？
- Flex 的主轴、交叉轴、基准尺寸、最小尺寸和溢出排错。
- Grid 的轨道、`minmax`、`auto-fit`、`auto-fill` 和响应式卡片。
- `position: sticky` 失效的常见原因。
- `overflow: clip`、`hidden`、`auto` 的差异。
- `display: contents` 的布局和可访问性风险。
- `:is`、`:where`、`:has`、容器查询和 cascade layer。
- CSS 变量的继承、回退值、主题切换和 JavaScript 修改。
- 伪元素、背景、渐变、遮罩和 CSS 三角形的可维护写法。
- 动画性能：transform / opacity、合成层、prefers-reduced-motion。
- 自适应图片、`srcset`、`picture`、`object-fit` 和 CLS。
- 字体加载、FOIT / FOUT、font-display。
- 表单 label、focus-visible、键盘操作、错误提示和 aria-live。
- 原生 dialog、下拉菜单、弹窗焦点陷阱和 Escape 关闭。
- 语义 HTML 与 ARIA 的边界：优先原生元素，不用 ARIA 伪造按钮。
- 响应式断点如何从内容和容器决定，而不是只按设备型号。
- 如何用 DevTools 定位样式覆盖、布局溢出、重排和重绘？

### 浏览器、网络、安全与存储

- 从 HTML 到 DOM、CSSOM、渲染树、布局、绘制、合成的完整过程。
- preload、preconnect、prefetch、defer、async 如何选择？
- HTTP/1.1、HTTP/2、HTTP/3 的连接和队头阻塞差异。
- DNS、TCP、TLS、QUIC 的基本连接过程。
- HTTP 方法语义、幂等、缓存、安全方法和状态码组合。
- CORS 预检、凭证、Origin、SameSite 与 CSRF 的关系。
- XSS 的反射型、存储型、DOM 型，以及 CSP、输出编码和 Trusted Types。
- Cookie、localStorage、sessionStorage、IndexedDB 的容量、生命周期和安全边界。
- Cache-Control、ETag、Last-Modified、stale-while-revalidate 和 CDN 更新。
- Service Worker 的安装、激活、缓存策略、版本更新和失控风险。
- Web Worker、SharedWorker、主线程与 OffscreenCanvas 的适用场景。
- WebSocket 与 SSE 的双向 / 单向通信、断线重连和鉴权。
- 文件上传、分片、断点续传、进度、类型校验和对象 URL 释放。
- 前端如何保护 Token、API Key、源码 map 和错误日志中的隐私信息？
- 前端监控：错误、性能、用户行为、采样、source map 和告警闭环。
- Core Web Vitals：LCP、INP、CLS 的含义、定位和验证方式。

### React

- state 是每次 render 的 snapshot，为什么 `setState` 后当前变量不会立即变？
- 多次 state 更新的批处理和函数式更新。
- 状态应该放在哪里：本地、最近共同父组件、Context、Redux、URL、服务端缓存？
- 派生数据为什么不应重复存成 state？
- `useEffect` 与事件处理的边界，什么时候根本不需要 Effect？
- Effect 依赖、清理函数、严格模式双调用和竞态。
- stale closure、定时器、事件监听和请求如何避免读旧值？
- controlled / uncontrolled 表单、文件输入和表单校验。
- `useReducer`、Context、selector 和局部订阅。
- React 19 的 actions、表单状态、`use` API 需要了解什么，项目是否使用？
- Suspense、lazy、错误边界、加载失败重试和路由级兜底。
- key、列表复用、组件状态迁移和 index key 风险。
- memo、useMemo、useCallback 的成本与性能验证。
- React 渲染、提交、Fiber、中断和优先级的面试级理解。
- hydration、SSR / SSG、客户端与服务端组件的基本边界。
- React 测试如何验证行为而不是内部实现？

### Vue

- Vue 3 Proxy 响应式、依赖收集、effect、computed 的缓存和失效。
- ref、reactive、toRef、toRefs、shallowRef、markRaw 的选择。
- 解构响应式对象为什么会丢响应式？
- computed、watch、watchEffect 的执行时机、深度监听和清理。
- nextTick 的使用场景和不能解决的问题。
- 组件 props 校验、emits、v-model 参数和修饰符。
- provide / inject、Pinia、URL、组件状态的边界。
- composable 如何设计输入、返回值、生命周期和请求取消？
- `v-if`、`v-show`、`keep-alive`、异步组件和缓存失效。
- Vue 2 响应式限制、数组和对象新增属性。
- Vue 2 / Vue 3 Diff、key、patch flag 的面试级理解。
- 插槽、作用域插槽、动态组件和组件递归。
- Vue Router 守卫、懒加载、滚动恢复、history 部署和权限。
- Vue 性能：大列表、虚拟滚动、稳定 props、组件拆分和依赖追踪。
- Vue Test Utils / E2E 测试如何验证交互和异步状态？

### 工程、测试与设计题

- Vite、Webpack、Rollup、Rolldown 的定位和版本差异；回答必须以项目实际版本为准。
- Loader、Plugin、插件钩子和资源处理边界。
- tree-shaking 失效的原因、sideEffects、动态导入和循环依赖。
- monorepo、包边界、依赖版本、锁文件和发布产物检查。
- 环境变量、开发代理、生产反向代理和 secret 管理。
- source map、错误堆栈、构建缓存和可复现构建。
- 单元测试、组件测试、集成测试、E2E 测试分别验证什么？
- 如何给异步请求、竞态、权限和错误边界写测试？
- Mock API、时间、随机数、IntersectionObserver、ResizeObserver 的方法。
- 前端系统设计：搜索联想、商品列表、购物车、仪表盘、聊天流、权限菜单。
- 组件库设计：主题、可访问性、受控 / 非受控、插槽、事件和版本兼容。
- 大列表、实时数据、离线、分页、缓存、重试、限流和降级的组合设计。
- 代码评审如何判断正确性、可维护性、性能、可访问性和安全性？
- 行为题：一次线上问题、一次失败、一次冲突、一次取舍、一次从零学习。

### 建议补充的代码题清单

至少准备可在 20～40 分钟内完成并解释取舍的实现：

- debounce / throttle（带取消和 flush）
- Promise.all / allSettled 简化实现
- 并发池、重试、超时、取消和请求去重
- EventEmitter
- 深拷贝与循环引用
- flatten、groupBy、去重、排序和分页
- LRU 缓存
- 虚拟列表核心计算
- 手写响应式最小实现
- React / Vue 搜索框与请求竞态保护
- 可访问的 Modal、Tabs、Dropdown
- 带校验、加载、失败、重试的表单

## 第二阶段：轻购项目题库

代码来源：

- 前端：`/Users/eli/Dev/mobile-shop`
- 服务端：`/Users/eli/Dev/mobile-shop-api`

已有题目覆盖智能导购、搜索联想、购物车嵌套状态、订单确认和项目介绍。还需要补齐：

### 业务与数据流

- 从登录到注册、首页、分类、搜索、详情、购物车、地址、确认订单、支付、订单列表的完整链路。
- 前端路由白名单、登录后 redirect、Token 失效和重复跳转。
- 前端 API 层如何统一 baseURL、超时、请求头、业务错误和 401。
- 服务端 JWT payload、7 天有效期、密码 bcrypt 哈希和注册校验。
- 用户数据如何通过 `user_id` 隔离收藏、购物车、地址和订单。
- 商品、SKU、库存、购物车项、订单和订单项的数据关系。
- 为什么订单保存 address snapshot，而不是只保存地址 ID？
- 购物车金额为什么必须由服务端重新查询和计算？
- 前端展示价格与最终订单价格如何防篡改？
- 下单时商品失效、SKU 下架、库存变化、地址删除分别如何处理？
- 提交订单的幂等键、重复点击和网络重试如何设计？当前实现有哪些限制，必须明确标记为待改进。
- 支付接口目前是模拟状态流转还是接入真实支付？不能夸大。

### AI 与推荐

- AI 解析 JSON 结构不合法、字段缺失、品类非法、预算异常怎么办？
- AI 失败为什么回退规则解析，规则解析有哪些覆盖边界？
- 推荐的品类、预算、需求覆盖、排除条件如何打分？
- 属性、标签、卖点、商品名的证据权重为什么不同？
- Top 5 如何保证商品族多样性，如何避免同一商品不同颜色占满结果？
- 预算内没有结果时为什么放宽约束，如何向用户说明？
- 如何评估推荐质量：固定场景、命中率、覆盖率、用户点击、转化和人工抽样。
- 搜索联想为什么同时需要防抖、取消、请求编号和缓存？
- AI key 为什么只能放服务端？前端代理如何做长度限制、超时、限流和错误映射？
- AI 生成商品卖点如何避免凭空编造商品属性？
- prompt 注入、恶意输入、敏感信息和模型返回 XSS 如何处理？

### 购物车、订单与并发

- 嵌套购物车如何扁平化，为什么不能直接修改计算结果之外的源对象？
- 全选、半选、删除、数量变化和总价请求如何保持一致？
- 快速连续点击加减时如何避免并发覆盖？现有按 basketId 的锁有什么边界？
- checkout 使用 basketIds 时服务端如何再次按 userId 查询，为什么不能信任前端商品和价格？
- `hydrateItems` 对缺失商品、下架商品和数量的处理是否符合业务预期？
- 订单事务包含哪些写操作？事务失败如何回滚？
- 库存目前在哪里扣减？是否存在超卖风险？如果没有实现，计划中要明确加入库存锁定 / 原子扣减和测试。
- 地址默认值更新为什么需要事务，如何保证一个用户只有一个默认地址？
- 订单提交成功但支付确认弹窗被取消时，页面和订单状态怎样恢复？
- 订单查询分页、N+1 查询和大订单列表如何优化？

### 移动端与工程质量

- rem、vw、PostCSS、Vant 组件尺寸和安全区适配。
- 搜索页历史记录的隐私、数量上限、去重和清空。
- 图片代理、接口代理、旧 chunk 加载失败后的只重试一次策略。
- 前端 `v-html` 的可信来源、清洗和 XSS 风险。
- 真机测试与 DevTools 模拟的差异。
- 商品详情、购物车、订单确认的 loading、空态、错误态和重复提交。
- 前端和服务端合同测试：响应包装、错误码、字段命名和版本兼容。

## 第三阶段：城市视图项目题库

代码来源：`/Users/eli/Dev/datapilot-rebuild`

已有题目覆盖图表配置、级联选择、仪表盘数据、图表尺寸、3D、权限和 Redux Toolkit。还需要补齐：

### 认证、权限与数据边界

- 登录响应、Token 持久化、刷新恢复和 401 清理的完整链路。
- 菜单过滤和路由保护为什么必须同时存在？
- `super_admin`、`admin`、`analyst`、`user` 的页面权限矩阵。
- URL 直接输入未授权页面时如何处理？未知路径为什么当前默认允许，是否需要改成明确拒绝？
- 浏览器修改角色字段为什么不能获得真实权限？后端必须再次校验。
- 用户、角色、菜单管理的保存、删除、失败和并发刷新。
- Redux persist 应保存哪些状态，为什么不能持久化整份业务数据？
- Token 过期时并发请求如何避免多个清理 / 跳转？

### 仪表盘、图表与数据源

- 首页多接口并行请求，单个失败是否影响其余卡片？
- 定时刷新如何停止、切后台是否继续、页面卸载如何清理？
- 图表配置的 datasource、table、xField、yField、aggregation 如何校验。
- 级联字段变化为什么清空下游字段，快速切换如何防止旧请求覆盖？
- 图表编辑回填与预览请求的状态机。
- ECharts 实例为什么不能在每次 render 重新 init？
- ResizeObserver 与 window.resize 的差异、节流、零尺寸初始化和销毁。
- bar、line、pie、scatter 的数据转换、排序、截断和空值处理。
- CSV 导出 BOM、引号、换行、逗号、中文、十万行内存和服务端导出。
- API 数据量增长后分页、聚合下推、缓存、虚拟化和接口超时。

### 地图、3D 与 AI 流式回答

- 高德 Key / 安全密钥 / 白名单 / 环境变量和泄露风险。
- 地图组件初始化失败、城市数据成功但环境数据部分失败时的降级。
- 地图实例、覆盖物、InfoWindow、事件监听和卸载清理。
- 2D 城市点位、环境 AQI 圆圈、模式切换和选中状态如何关联。
- React Three Fiber 与 Three.js 的关系，柱高、颜色、动画和相机控制。
- 城市数量增加后如何做实例化、LOD、分页、聚合和设备降级。
- WebGL 不可用、低端设备卡顿和 prefers-reduced-motion 的处理。
- AI 流式接口的 SSE / chunk 格式、TextDecoder 分片边界、残留 buffer 和结束事件。
- AI 流式请求中途断网、重复发送、取消、服务端保存半截消息和重试。
- thinking / fallback / text 三类事件是否应对用户展示，如何避免内部思考内容泄露。
- 会话创建、消息保存和流式生成失败时的一致性。

### 工程与测试

- `lazyWithRetry` 和 ErrorBoundary 的分工，为什么只能自动刷新一次。
- dynamic import 失败后旧 HTML、CDN、Service Worker 和 chunk 文件的关系。
- 如何测试菜单权限矩阵、401、图表空数据、接口部分失败和地图缺少 Key。
- 如何测试 ResizeObserver、ECharts 和 WebGL，不依赖真实浏览器环境。
- 如何为 AI 流式解析构造分片、乱序、错误 JSON 和 `[DONE]` 测试数据。
- 生产配置中的 AMap key、AI endpoint、API 代理和错误日志脱敏。

## 第四阶段：智服工单项目题库

代码来源：

- `/Users/eli/Resumes/after-sales-work-order/frontend`
- `/Users/eli/Resumes/after-sales-work-order/server`

已有题目覆盖项目介绍、SLA、状态机、角色限制和并发冲突。还需要补齐：

### 业务模型与权限

- 客户、设备、工单、分类、用户、角色、操作记录的关系。
- 创建工单时客户、设备、分类的联动和服务端 Zod 校验。
- engineer 只能看到和操作自己被分配的工单，admin / service 的可见范围。
- 列表筛选、关键字、状态、优先级、负责人、分类、逾期和日期范围如何组合。
- 前端隐藏按钮和服务端 allowRoles / visibility 的区别。
- JWT 中的 id、username、role 如何使用，用户状态停用后旧 Token 如何处理。
- 客户与设备归属如何校验，避免提交一个客户的设备给另一个客户。
- 管理员修改用户角色、状态、密码时的权限和审计。

### 状态机、并发和 SLA

- 每个状态允许的转移、角色允许的转移为什么是两层校验。
- `FOR UPDATE`、version 条件更新、事务和操作记录的先后顺序。
- 两个人同时点击开始处理、完成、取消时的具体时序。
- 页面旧版本提交如何识别，为什么前端禁用按钮不够。
- 状态更新成功但操作记录失败如何处理；是否需要同一事务和补偿机制。
- SLA 起算、暂停、恢复、跨天、周末、时区和节假日规则。
- 逾期由数据库 NOW 计算和前端倒计时显示各自负责什么。
- 一百条工单的倒计时如何避免一百个定时器。
- 页面长时间打开、后台挂起、系统休眠后如何重新计算。
- 逾期后的排序、催办、通知和审计目前是否实现。

### 安全、可靠性和运维

- bcrypt 密码哈希、密码长度规则、登录错误信息和登录限流。
- originGuard 的用途、代理密钥轮换和开发环境差异。
- Helmet、CORS、请求体限制、错误处理中不能泄露 SQL 或堆栈。
- SQL 参数化、分页上限、关键字 LIKE、排序字段白名单。
- 上传、手机号、客户资料和日志中的个人信息保护。
- 数据库连接池、事务释放、超时和服务不可用时的前端反馈。
- 备份 service / timer 的用途、恢复演练和备份有效性验证。
- 前端 Axios 401 处理、重复跳转、错误 toast 和页面恢复。
- Vue 2 / Vuex 的局部状态、请求竞态和组件销毁清理。

### 测试与现场演示

- 状态转移矩阵的单元测试和角色矩阵测试。
- controller 的鉴权、参数校验、数据可见性和错误码测试。
- 并发更新的数据库集成测试，而不是只测纯函数。
- SLA 边界：恰好到期、跨天、暂停、已完成、时区变化。
- 前端创建工单的客户切换清空设备、加载失败和重复提交。
- 现场演示一条完整工单：创建、分配、工程师处理、回访、完成和审计记录。
- 明确哪些功能在代码中存在但没有线上验证，不能把“代码路径存在”说成“生产已证明”。

## 第五阶段：题目写作与去重规则

### 每道题的固定结构

```markdown
---
id: 唯一 ID
title: 面试官会问的问题
aliases: [常见问法]
category: 分类
projects: [只填写本用户拥有的项目]
keywords: [检索词]
priority: high
---

## 核心回答

30 秒到 2 分钟内能说完，先结论，再依据，再边界。

## 追问：具体边界问题

只回答这个追问，给出原因、实现和限制。

## 追问：如何验证？

说明测试、DevTools、日志或代码证据。
```

### 口语质量规则

- 少用“必说”“加分”“这题考点”“报菜名”“一句话总结”。
- 不把百科定义堆在一起；每个回答先说自己理解，再给一个具体例子。
- 只有实际做过的内容才使用“我在项目里”；纯知识使用“可以这样理解”“常见做法是”。
- 对没做过的事情直接标注“了解原理但没有在项目中实现”，给出可以验证的方向。
- 每个追问都必须有独立答案，避免答案只有关键词。
- 代码证据必须存在于对应用户自己的项目，路径不存在或无法核对时不写。
- 技术版本可能变化的内容（例如 Vite 底层构建器）写清版本和核对方法，不把旧教程当永远正确。

### 去重规则

- 标题、aliases、keywords 做规范化后检查近似重复。
- 同一用户内，相同问题只保留一个主文件，新增内容并入追问。
- 通用知识允许在两个用户题库出现，但回答正文使用中性示例，不能混入对方项目。
- 项目题必须通过 `projects` 白名单和正文归属检查。
- 个人资料题必须检查姓名、学校、奖项、网站、代码路径和第一人称经历。
- 每次构建运行题库归属检查；每次新增项目题运行对应项目测试或至少做代码路径核对。

## 第六阶段：执行顺序

1. 先补 JavaScript、TypeScript、浏览器网络和测试代码题，解决通用面试的最大缺口。
2. 再补 React 和 Vue 的状态、异步、性能和组件设计题。
3. 分别按轻购、城市视图、智服工单的代码目录补项目追问，每个项目先补业务链路，再补实现细节和边界。
4. 为新增题目补测试：解析、重复 ID、项目归属、跨用户污染、关键题目存在性。
5. 对所有回答做一次口语化审校，删除模板化面试话术。
6. 运行题库检查、完整测试、三个项目可运行的 type-check / build / test；不能运行的项目记录具体原因。
7. 最后做一次人工模拟面试：每个通用主题抽题，每个项目从介绍连续追问五层，记录仍答不出来的题再回填。

## 验收标准

- `default` 的 137 道原有知识点和项目题不被覆盖，新增内容只在明确允许的纯技术题范围内修改。
- Aaron 与 default 的项目、身份、代码证据和第一人称经历完全隔离。
- 通用题覆盖上述主题，每个主题都有概念、实践、边界和验证题。
- 三个项目各自至少有：项目介绍、数据模型、接口链路、核心难点、异常降级、权限安全、性能、测试、部署、取舍和未实现项。
- 题目没有只换标题的重复，也没有没有答案的追问清单。
- 所有答案能在真实面试中自然说出，且不会把“看过代码”包装成“线上做过”。
- 构建前自动执行归属校验；测试失败时不得发布题库。

## 需要人工确认的事实

以下内容在写入最终回答前必须以当前代码、测试或你的明确说明为准：

- 轻购是否真实扣库存、是否真实接入支付平台、AI 推荐是否有线上用户反馈。
- 城市视图的后端权限是否与前端权限矩阵完全一致，AI 流式服务是否保存半截消息。
- 智服工单的 SLA 是否支持周末、节假日、暂停和通知，数据库并发更新是否有集成测试。
- 三个项目的部署地址、上线状态、性能数字、用户数量和团队分工。
- 任何学校、奖项、网站、工作经历和代码路径。

